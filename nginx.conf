server {
        #implemented by default, change if you need different ip or port
        listen *:80;
        server_name codimd.org www.codimd.org;
        return 302 https://github.com/hackmdio/codimd;
}
